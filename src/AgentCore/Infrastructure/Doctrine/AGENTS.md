# Doctrine integration

Stage 03 introduces the baseline persistence schema migration:

- `Migrations/Version20260418000100.php`

It creates core tables for runs, commands, canonical events, hot prompt state, turn index, and tool jobs.
