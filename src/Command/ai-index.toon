spec: agent-core.ai-docs/v1
namespace: Command
fqcn: "Ineersa\\AgentCore\\Command"
description: Symfony console commands for operational management of the agent loop.
files[2]{file,type,responsibility,docs}:
  AgentLoopHealthCommand.php,final class,"Console command `agent-loop:health` — checks agent loop subsystem health (stores, buses, connectivity).",docs/AgentLoopHealthCommand.md
  AgentLoopResumeStaleRunsCommand.php,final class,"Console command `agent-loop:resume-stale-runs` — finds stale running runs after worker restart, rebuilds hot prompt state if missing, and dispatches AdvanceRun to resume them.",docs/AgentLoopResumeStaleRunsCommand.md
