# TUI configuration.
#
# Theme selection and theme search paths are driven by Hatfield settings
# (~/.hatfield/settings.yaml and <project>/.hatfield/settings.yaml).
#
# To change the active theme, edit your Hatfield settings file.
# See docs/settings.md and docs/tui-architecture.md for details.
