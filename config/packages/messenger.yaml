# Messenger buses are now configured in config/packages/framework.yaml
# under framework.messenger. This file is intentionally left minimal.
#
# Bus definitions:
#   agent.command.bus  — orchestrator handlers (StartRun, ApplyCommand, etc.)
#   agent.execution.bus — worker handlers (ExecuteLlmStep, ExecuteToolCall)
